import React, { useState, useEffect } from 'react';
import { 
  Settings, 
  Image as ImageIcon, 
  Layers, 
  Edit3, 
  X, 
  Play, 
  Upload, 
  ChevronRight, 
  Trash2, 
  Search,
  CheckCircle2,
  AlertCircle
} from 'lucide-react';

// 模擬原始資料結構
const MOCK_PRESETS = [
  {
    id: '1',
    name: '夢幻人像 (Dreamy Portrait)',
    category: 't2i',
    previewImageUrl: 'https://images.unsplash.com/photo-1544005313-94ddf0286df2?auto=format&fit=crop&q=80&w=300&h=300',
    parameters: [
      { nodeId: '6', inputName: 'text', displayName: '正面提示詞', defaultValue: 'A beautiful portrait, cinematic lighting, 8k', visible: true },
      { nodeId: '3', inputName: 'steps', displayName: '步數', defaultValue: 25, visible: true },
      { nodeId: '3', inputName: 'cfg', displayName: 'CFG Scale', defaultValue: 7.5, visible: true },
    ]
  },
  {
    id: '2',
    name: '賽博龐克城市 (Cyberpunk City)',
    category: 't2i',
    previewImageUrl: 'https://images.unsplash.com/photo-1605142859862-978be7eba909?auto=format&fit=crop&q=80&w=300&h=300',
    parameters: [
      { nodeId: '6', inputName: 'text', displayName: '場景描述', defaultValue: 'Neo-tokyo, raining, neon lights', visible: true },
      { nodeId: '3', inputName: 'steps', displayName: '步數', defaultValue: 30, visible: true },
    ]
  },
  {
    id: '3',
    name: '老照片修復',
    category: 'edit',
    previewImageUrl: 'https://images.unsplash.com/photo-1516035069371-29a1b244cc32?auto=format&fit=crop&q=80&w=300&h=300',
    parameters: [
      { nodeId: '10', inputName: 'denoise', displayName: '降噪強度', defaultValue: 0.5, visible: true },
    ]
  }
];

const App = () => {
  const [isOpen, setIsOpen] = useState(false);
  const [activeTab, setActiveTab] = useState('t2i');
  const [selectedPreset, setSelectedPreset] = useState(null);
  const [isGenerating, setIsGenerating] = useState(false);
  const [statusMsg, setStatusMsg] = useState('');

  const tabs = [
    { id: 't2i', label: '文生圖', icon: <ImageIcon size={18} /> },
    { id: 'i2i', label: '圖生圖', icon: <Layers size={18} /> },
    { id: 'edit', label: '圖片編輯', icon: <Edit3 size={18} /> },
    { id: 'admin', label: '管理員', icon: <Settings size={18} />, color: 'text-orange-400' },
  ];

  const filteredPresets = MOCK_PRESETS.filter(p => p.category === activeTab);

  const handleGenerate = () => {
    setIsGenerating(true);
    setStatusMsg('正在連接 ComfyUI 伺服器...');
    
    // 模擬生成過程
    setTimeout(() => {
      setStatusMsg('工作流已發送！請等待生成結果。');
      setTimeout(() => {
        setIsGenerating(false);
        setStatusMsg('');
      }, 3000);
    }, 1500);
  };

  return (
    <div className="min-h-screen bg-[#09090b] text-zinc-100 font-sans antialiased p-8">
      {/* 頁面背景裝飾 */}
      <div className="fixed top-0 left-0 w-full h-full overflow-hidden pointer-events-none opacity-20">
        <div className="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-emerald-500 rounded-full blur-[120px]" />
        <div className="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-blue-600 rounded-full blur-[120px]" />
      </div>

      {/* 1. 🍌 現代化懸浮按鈕 */}
      <button 
        onClick={() => setIsOpen(true)}
        className="fixed bottom-10 right-10 w-16 h-16 bg-emerald-500 hover:bg-emerald-400 text-white rounded-2xl flex items-center justify-center shadow-[0_0_20px_rgba(16,185,129,0.4)] transition-all duration-300 hover:scale-110 active:scale-95 z-50 group"
      >
        <span className="text-3xl group-hover:rotate-12 transition-transform">🍌</span>
      </button>

      {/* 2. 主視窗 (Modal) */}
      {isOpen && (
        <div className="fixed inset-0 z-[100] flex items-center justify-center p-4 md:p-8 animate-in fade-in duration-300">
          <div 
            className="absolute inset-0 bg-black/60 backdrop-blur-sm"
            onClick={() => setIsOpen(false)}
          />
          
          <div className="relative w-full max-w-6xl h-[85vh] bg-zinc-900/90 border border-zinc-800 rounded-3xl shadow-2xl overflow-hidden flex flex-col backdrop-blur-xl">
            {/* Header */}
            <header className="px-6 py-4 border-b border-zinc-800 flex items-center justify-between bg-zinc-900/50">
              <div className="flex items-center gap-8">
                <div className="flex items-center gap-2">
                  <span className="text-2xl">🍌</span>
                  <span className="font-bold text-lg tracking-tight bg-gradient-to-r from-emerald-400 to-emerald-600 bg-clip-text text-transparent">
                    TK Toolkit Pro
                  </span>
                </div>
                
                <nav className="flex items-center gap-1">
                  {tabs.map(tab => (
                    <button
                      key={tab.id}
                      onClick={() => {
                        setActiveTab(tab.id);
                        setSelectedPreset(null);
                      }}
                      className={`flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-medium transition-all ${
                        activeTab === tab.id 
                        ? 'bg-zinc-800 text-emerald-400 shadow-inner' 
                        : 'text-zinc-500 hover:text-zinc-300 hover:bg-zinc-800/50'
                      } ${tab.color || ''}`}
                    >
                      {tab.icon}
                      {tab.label}
                    </button>
                  ))}
                </nav>
              </div>
              
              <button 
                onClick={() => setIsOpen(false)}
                className="p-2 hover:bg-zinc-800 rounded-full text-zinc-500 hover:text-white transition-colors"
              >
                <X size={20} />
              </button>
            </header>

            {/* Content Body */}
            <div className="flex-1 flex overflow-hidden">
              {activeTab !== 'admin' ? (
                <>
                  {/* Sidebar (Presets List) */}
                  <aside className="w-72 border-r border-zinc-800 bg-zinc-900/30 overflow-y-auto p-4 space-y-4">
                    <div className="relative mb-6">
                      <Search className="absolute left-3 top-2.5 text-zinc-600" size={16} />
                      <input 
                        type="text" 
                        placeholder="搜尋預設..." 
                        className="w-full bg-zinc-800/50 border border-zinc-700/50 rounded-xl py-2 pl-10 pr-4 text-sm focus:outline-none focus:border-emerald-500/50 transition-colors"
                      />
                    </div>

                    <div className="space-y-3">
                      <h3 className="px-2 text-xs font-semibold text-zinc-500 uppercase tracking-wider">常用預設</h3>
                      {filteredPresets.map(preset => (
                        <div 
                          key={preset.id}
                          onClick={() => setSelectedPreset(preset)}
                          className={`group relative p-3 rounded-2xl cursor-pointer transition-all border ${
                            selectedPreset?.id === preset.id 
                            ? 'bg-emerald-500/10 border-emerald-500/30 shadow-lg' 
                            : 'bg-zinc-800/20 border-transparent hover:border-zinc-700 hover:bg-zinc-800/50'
                          }`}
                        >
                          <div className="flex items-center gap-3">
                            <img 
                              src={preset.previewImageUrl} 
                              alt="" 
                              className="w-12 h-12 rounded-lg object-cover bg-zinc-900 ring-1 ring-white/5"
                            />
                            <div className="flex-1 min-w-0">
                              <p className={`text-sm font-semibold truncate ${selectedPreset?.id === preset.id ? 'text-emerald-400' : 'text-zinc-200'}`}>
                                {preset.name}
                              </p>
                              <p className="text-xs text-zinc-500 truncate">Node 3, 6, 10</p>
                            </div>
                            <ChevronRight size={14} className={`text-zinc-600 group-hover:translate-x-1 transition-transform ${selectedPreset?.id === preset.id ? 'text-emerald-500' : ''}`} />
                          </div>
                        </div>
                      ))}
                      {filteredPresets.length === 0 && (
                        <div className="text-center py-10">
                          <ImageIcon className="mx-auto text-zinc-700 mb-2" size={32} />
                          <p className="text-sm text-zinc-600">目前暫無預設項目</p>
                        </div>
                      )}
                    </div>
                  </aside>

                  {/* Main Interaction Panel */}
                  <main className="flex-1 overflow-y-auto bg-zinc-950/20">
                    {selectedPreset ? (
                      <div className="p-8 max-w-4xl mx-auto space-y-8 animate-in slide-in-from-right-4 duration-300">
                        {/* Preview Area */}
                        <div className="relative group rounded-3xl overflow-hidden bg-zinc-900 border border-zinc-800 aspect-video shadow-2xl">
                          <img 
                            src={selectedPreset.previewImageUrl} 
                            className="w-full h-full object-contain opacity-60 group-hover:opacity-100 transition-opacity"
                            alt="Preview"
                          />
                          <div className="absolute inset-0 flex items-center justify-center bg-black/40 backdrop-blur-[2px] opacity-0 group-hover:opacity-100 transition-opacity">
                            <span className="text-xs font-medium bg-black/60 px-3 py-1.5 rounded-full border border-white/10">查看原始圖片</span>
                          </div>
                          <div className="absolute top-4 left-4 bg-emerald-500/20 backdrop-blur-md border border-emerald-500/30 px-3 py-1 rounded-full flex items-center gap-2">
                            <div className="w-1.5 h-1.5 bg-emerald-400 rounded-full animate-pulse" />
                            <span className="text-[10px] font-bold text-emerald-400 uppercase tracking-widest">Active Preset</span>
                          </div>
                        </div>

                        {/* Parameter Form */}
                        <div className="bg-zinc-900/50 border border-zinc-800 rounded-3xl p-6 shadow-xl">
                          <h4 className="text-sm font-bold text-zinc-400 mb-6 flex items-center gap-2">
                            <Play size={14} className="text-emerald-500" /> 參數設定
                          </h4>
                          
                          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                            {selectedPreset.parameters.map((param, idx) => (
                              <div key={idx} className="space-y-2">
                                <label className="text-xs font-medium text-zinc-500 px-1">{param.displayName}</label>
                                <input 
                                  type="text"
                                  defaultValue={param.defaultValue}
                                  className="w-full bg-zinc-950/50 border border-zinc-800 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-emerald-500/20 focus:border-emerald-500/50 transition-all text-zinc-200"
                                />
                              </div>
                            ))}
                          </div>

                          {/* Action Bar */}
                          <div className="mt-10 pt-6 border-t border-zinc-800 flex flex-col items-end gap-3">
                            {statusMsg && (
                              <div className={`flex items-center gap-2 text-xs px-4 py-2 rounded-lg ${statusMsg.includes('Error') ? 'bg-red-500/10 text-red-400' : 'bg-emerald-500/10 text-emerald-400'}`}>
                                {statusMsg.includes('Error') ? <AlertCircle size={14} /> : <CheckCircle2 size={14} />}
                                {statusMsg}
                              </div>
                            )}
                            <button 
                              onClick={handleGenerate}
                              disabled={isGenerating}
                              className={`flex items-center justify-center gap-3 w-full md:w-auto min-w-[200px] px-8 py-3.5 rounded-2xl font-bold text-sm transition-all shadow-lg ${
                                isGenerating 
                                ? 'bg-zinc-800 text-zinc-500 cursor-not-allowed' 
                                : 'bg-emerald-500 hover:bg-emerald-400 text-zinc-950 hover:shadow-emerald-500/20 active:scale-95'
                              }`}
                            >
                              {isGenerating ? (
                                <>
                                  <div className="w-4 h-4 border-2 border-zinc-500 border-t-transparent rounded-full animate-spin" />
                                  生成中 (Queuing...)
                                </>
                              ) : (
                                <>
                                  <Play size={18} fill="currentColor" />
                                  生成圖片 (Generate)
                                </>
                              )}
                            </button>
                          </div>
                        </div>
                      </div>
                    ) : (
                      <div className="h-full flex flex-col items-center justify-center text-zinc-600 animate-in fade-in zoom-in duration-500">
                        <div className="w-20 h-20 rounded-3xl bg-zinc-900 border border-zinc-800 flex items-center justify-center mb-4">
                          <ImageIcon size={40} className="text-zinc-700" />
                        </div>
                        <p className="text-lg font-medium">請從左側選擇一個工作流預設</p>
                        <p className="text-sm">開始您的創作旅程</p>
                      </div>
                    )}
                  </main>
                </>
              ) : (
                /* Admin Panel View */
                <main className="flex-1 overflow-y-auto p-10 animate-in fade-in duration-300">
                  <div className="max-w-5xl mx-auto space-y-10">
                    <div className="flex items-center justify-between">
                      <div>
                        <h2 className="text-2xl font-bold text-white mb-1">工作流管理員</h2>
                        <p className="text-sm text-zinc-500">配置預設工作流、參數可見性與默認值。</p>
                      </div>
                      <button className="flex items-center gap-2 bg-emerald-500/10 hover:bg-emerald-500/20 text-emerald-400 border border-emerald-500/30 px-4 py-2 rounded-xl text-sm font-semibold transition-all">
                        <Upload size={16} /> 上傳工作流 JSON
                      </button>
                    </div>

                    <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
                      {/* Left: Preset List */}
                      <div className="lg:col-span-1 space-y-4">
                        <h3 className="text-xs font-bold text-zinc-600 uppercase tracking-widest px-2">已保存預設 ({MOCK_PRESETS.length})</h3>
                        <div className="bg-zinc-900/50 border border-zinc-800 rounded-2xl divide-y divide-zinc-800 overflow-hidden">
                          {MOCK_PRESETS.map(p => (
                            <div key={p.id} className="p-4 hover:bg-zinc-800/50 transition-colors flex items-center justify-between group">
                              <div className="flex items-center gap-3">
                                <div className="w-10 h-10 rounded-lg bg-zinc-800 overflow-hidden">
                                  <img src={p.previewImageUrl} alt="" className="w-full h-full object-cover" />
                                </div>
                                <div>
                                  <p className="text-sm font-semibold text-zinc-200">{p.name}</p>
                                  <p className="text-[10px] text-zinc-500 uppercase">{p.category}</p>
                                </div>
                              </div>
                              <button className="text-zinc-600 hover:text-red-400 opacity-0 group-hover:opacity-100 transition-all p-1">
                                <Trash2 size={16} />
                              </button>
                            </div>
                          ))}
                        </div>
                      </div>

                      {/* Right: Detailed Config Form */}
                      <div className="lg:col-span-2 bg-zinc-900/50 border border-zinc-800 rounded-3xl p-8 space-y-8 shadow-inner">
                         <div className="flex items-center gap-3 text-emerald-400 mb-2">
                           <Settings size={20} />
                           <h3 className="font-bold">新增/編輯 預設內容</h3>
                         </div>
                         
                         <div className="space-y-6">
                           <div className="grid grid-cols-2 gap-6">
                             <div className="space-y-2">
                               <label className="text-xs font-semibold text-zinc-500 px-1">顯示名稱 (Display Name)</label>
                               <input type="text" placeholder="例如：寫實人像 V2" className="w-full bg-zinc-950/50 border border-zinc-800 rounded-xl px-4 py-2 text-sm text-white" />
                             </div>
                             <div className="space-y-2">
                               <label className="text-xs font-semibold text-zinc-500 px-1">分類 (Category)</label>
                               <select className="w-full bg-zinc-950/50 border border-zinc-800 rounded-xl px-4 py-2 text-sm text-white focus:outline-none">
                                 <option value="t2i">文生圖 (T2I)</option>
                                 <option value="i2i">圖生圖 (I2I)</option>
                                 <option value="edit">圖片編輯 (Edit)</option>
                               </select>
                             </div>
                           </div>

                           <div className="space-y-4">
                              <h4 className="text-[10px] font-bold text-zinc-600 uppercase tracking-[0.2em] pt-4">節點與參數配置</h4>
                              <div className="space-y-4">
                                {/* Mock Node Config */}
                                {[3, 6].map(node => (
                                  <div key={node} className="bg-zinc-950/30 border border-zinc-800 rounded-2xl overflow-hidden">
                                    <div className="bg-zinc-800/40 px-4 py-2 text-[10px] font-bold text-zinc-400 flex justify-between">
                                      <span>NODE ID: {node}</span>
                                      <span className="text-emerald-500/60 uppercase">KSampler</span>
                                    </div>
                                    <div className="p-4 space-y-3">
                                      <div className="flex items-center gap-4 text-sm">
                                        <input type="checkbox" className="accent-emerald-500" defaultChecked />
                                        <span className="text-zinc-300 w-24">seed</span>
                                        <input type="text" placeholder="顯示名稱" className="flex-1 bg-zinc-900 border border-zinc-700 rounded-lg px-3 py-1 text-xs" defaultValue="隨機種子" />
                                        <input type="text" placeholder="默認值" className="w-32 bg-zinc-900 border border-zinc-700 rounded-lg px-3 py-1 text-xs" defaultValue="0" />
                                      </div>
                                    </div>
                                  </div>
                                ))}
                              </div>
                           </div>
                           
                           <div className="flex justify-end gap-3 pt-6">
                             <button className="px-6 py-2 rounded-xl text-sm font-semibold text-zinc-500 hover:text-white transition-colors">取消</button>
                             <button className="px-8 py-2 bg-emerald-500 text-zinc-950 rounded-xl text-sm font-bold shadow-lg shadow-emerald-500/10 hover:bg-emerald-400 transition-all">保存預設</button>
                           </div>
                         </div>
                      </div>
                    </div>
                  </div>
                </main>
              )}
            </div>
          </div>
        </div>
      )}
    </div>
  );
};

export default App;